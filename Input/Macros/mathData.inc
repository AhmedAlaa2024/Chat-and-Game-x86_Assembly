;=============================================
;         Arithmatic Operations Strings       |
;=============================================
ADD_STR  DB  'ADD '
ADC_STR  DB  'ADC '
SUB_STR  DB  'SUB '
SBB_STR  DB  'SBB '
XOR_STR  DB  'XOR '
AND_STR  DB  'AND '
OR_STR   DB  'OR '
;=============================================
;         Bitwise Operations Strings          |
;=============================================
SHR_STR DB 'SHR '
SHL_STR DB 'SHL '
SAR_STR DB 'SAR '
ROR_STR DB 'ROR '
RCL_STR DB 'RCL '
RCR_STR DB 'RCR '
ROL_STR DB 'ROL '
;=============================================
;              REGISTERS STRINGS              |
;=============================================
AX_STR DB 'AX'
AL_STR DB 'AL'
AH_STR DB 'AH'
BX_STR DB 'BX'
BL_STR DB 'BL'
BH_STR DB 'BH'
CX_STR DB 'CX'
CL_STR DB 'CL'
CH_STR DB 'CH'
DX_STR DB 'DX'
DL_STR DB 'DL'
DH_STR DB 'DH'
SI_STR DB 'SI'
DI_STR DB 'DI'
BP_STR DB 'BP'
SP_STR DB 'SP'
;=============================================
;              LOCAL-PLAYER DATA              |
;=============================================

;=============================================
;             NETWORK-PLAYER DATA             |
;=============================================

;=============================================
;             Miscellaneous Data              |
;=============================================
OP_FLAG  DB  ?